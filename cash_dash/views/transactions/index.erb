<table>
  <tr>
    <th>Merchant</th>
    <th>Category</th>
    <th>Amount</th>
  </tr>
  <% @transactions.each do |transaction| %>
    <tr>
      <td><%= transaction.merchant().name %></td>
      <td><%= transaction.category().name %></td>
      <td>£<%= transaction.amount_pounds_to_display() %>.
      <%= transaction.amount_pence_to_display() %></td>
    </tr>
  <% end %>
</table>
