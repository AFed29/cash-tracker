<a href="/transactions/total">Total spent</a>
<table>
  <tr>
    <th>Merchant</th>
    <th>Category</th>
    <th>Amount</th>
  </tr>
  <% @transactions.each do |transaction| %>
    <tr>
      <td><%= transaction.merchant().name %></td>
      <td><%= transaction.category().name %></td>
      <td>£<%= Transaction.amount_pounds_to_display(transaction.amount) %>.
      <%= Transaction.amount_pence_to_display(transaction.amount) %></td>
    </tr>
  <% end %>
</table>
